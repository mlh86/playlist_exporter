Takes an M3U8 Playlist file as input and copies the playlist items to the specified
export folder, which can be a relative or absolute directory path that need not
already exist. The exported files are prepended with a numeric sequence so
that they follow the order of the playlist.